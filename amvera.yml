meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12" 

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  command: gunicorn DjangoWebProject_lab4.wsgi:application --bind 0.0.0.0:80
  persistenceMount: /data
  containerPort: 80